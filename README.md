Eopp Engine
#
To start working on this project, start with running the following script:
* git submodule update --init --recursive

On Linux, run:
* chmod +x run.sh
* ./run.sh

On Windows, run:
* run.bat